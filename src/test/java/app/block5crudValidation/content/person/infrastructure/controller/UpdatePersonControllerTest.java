package app.block5crudValidation.content.person.infrastructure.controller;

class UpdatePersonControllerTest {

//    @Test
//    void update() {
//        // Arrange
//        UpdatePersonUseCase updatePersonUseCaseMock = mock(UpdatePersonUseCase.class);
//        UpdatePersonController controller = new UpdatePersonController(updatePersonUseCaseMock);
//
//        PersonUpdateInputDto inputDto = new PersonUpdateInputDto();
//        // set up inputDto with test data
//
//        when(updatePersonUseCaseMock.update(PersonEntityMapper.INSTANCE.toEntity(inputDto))).thenReturn(1L); // Assuming the update returns 1
//
//        // Act
//        ResponseEntity<Long> response = controller.update(inputDto);
//
//        // Assert
//        assertEquals(HttpStatus.CREATED, response.getStatusCode());
//        assertEquals(1L, response.getBody()); // Assuming 1 is the expected personId after update
//    }
}
